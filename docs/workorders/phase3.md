# 発注書: フェーズ 3(淘汰と成長)(2026-07-29)

> 進め方の共通ルール(プロセス・レビュー体制・記録の残し方)は [implementation-workorder.md](../implementation-workorder.md) を参照。本書はフェーズ 3 の発注内容だけを書く。

フェーズ 1・2 と同じく「プロセス1 → 方向性レビュー → プロセス2 → 完了レビュー」で回します。担当は **E8(評価と淘汰)・E9(優先順位・人気度)・E10 のフェーズ 3 分(OP-03・04)**。着手順の設計意図はバックログ §4 のフェーズ 3 の段落が正です(評価 EV → 淘汰 EV-03 → 人気度連動の優先順位 PR → 観測 OP)。

## 着手前の前提

- **設計書は承認済みです**(2026-07-27 開発者レビュー。ヘッダの状態表記は 2026-07-29 に同期済み)。E08 は**冒頭の改訂反映ノート**(セット途中打ち切りの追加)から読んでください。
- **依存ストーリーはすべて完了済みです。** EV-01←GE-05、EV-02←CX-06、EV-03←CX-04、PR-01←CX-03、OP-03/04 の依存する CX/RP 系は main 統合済み(現況の正は `docs/impl-progress.md`)。フェーズ 3 内の依存(EV-01→02→03、PR-01→02、OP-03→04)だけ管理すれば足ります。
- **PR-01 の意味論は E1 実装時に一部先行実装済みです。** core に `priority/effects.ts` があり、`PriorityKey = { score, activatedAt: epochMs, ruleId }`(E09 正準)・`ruleId` のコード単位辞書順比較まで E1 の完了レビューで E09 に揃えてあります(impl-progress「並行進行: E1」節)。PR-01 は**ゼロから書かず、既存実装と E09 §3.1 の突き合わせから**始めてください。
- **E11 の本有効化がフェーズ 3 の出口に含まれます。** RV-01(並び順=優先度)・RV-02(図鑑の人気度・優先度・排除済み表示)は「E9/E8 のデータが揃ったら本有効化する」後続ゲート付きで実装済みです(impl-progress「E11 RV-01・RV-02」節)。PR-02・EV-03 の完了時に、この本有効化までを受け入れ範囲に含めます。
- **decision-log は D 節が本丸です。** 加えて B・E 節も「宛先」欄で自分のストーリー ID を検索してください。特に:
  - **D-1**(排除パラメータ θ=0.70 / N_min=10 / z=1.96)・**D-2**(人気度 Beta(5,5))・**D-3**(fun_rate 定義)は未決ですが、いずれも**設計書の初期値・定義のまま仮値で着手可**です(D-1 は settings で変更可能にする前提)。開発者承認は早めに依頼してください。D-6(手動復活の運営認証)は決定済み(当面 CLI+SSH)。
  - **B-3**(セット途中打ち切りの UI 文言と games_played<3 の集計扱い)は EV-01 の着手時に決定が必要です。**B-4**(アクションログの保存先・保持期間)は OP-03 宛て。
  - **E-5**(`game_sets`・`set_rules(was_active, did_fire)`・`set_participants` の書き込みを E1/E3 へ足す)・**E-6**(評価保存 tx 内で `recomputePopularity` を呼ぶ契約)・**E-7**(`set_rule_snapshots` と `set_rules` の統合可否)は**未反映の Epic 間連絡**です。着手時に、宛先設計書へ反映するか実装時読み替えにするかを決めてから進めてください。E-12(E12 改訂の本文反映)も E08 該当節が対象です。
- **評価 UI は情報量規律の適用対象です。** 評価画面(画面 5b)に説明文を置かないこと(UI文言・情報量ガイドの削除テスト・置き場テスト。「低評価が集まると排除される」の知識は図鑑の「排除済み」行に置く — なお「あそびかた」ページは C-13 で保留・導線非表示のため、置き場としては当面使えません)。

## 担当範囲

| # | Epic / ストーリー | 関係する未決 | レビューの重点 |
|---|---|---|---|
| 1 | **E8** 評価の受け皿(EV-01 → EV-02) | B-3(着手時に決定)・E-5(スキーマ書き込みの所在) | セット単位評価(fun/neutral/boring)とルール単位評価(up/down)が E08 §2 のデータモデルどおり保存されること。**評価はスキップ可能で、押した時点で送信済み**(確認ボタンを増やさない)。打ち切りセットでも評価が成立すること。AI 席を評価者・`set_participants` に入れないこと |
| 2 | **E9** 優先順位機構(PR-01) | E-7(E8 のテーブルとの統合可否) | 既存 core 実装(`priority/effects.ts`)と E09 §3.1 の意味論(変換系フックは高優先度が後勝ち・Effect 矛盾は最高優先度のみ・同点時の扱い)の一致。競合解決ログが追跡できること。**契約(Effect 語彙・フック)を変えないこと** |
| 3 | **E8** 自動排除(EV-03) | D-1(仮値で着手可・settings 化)・D-6(決定済み: CLI) | 排除判定が E08 §3.3 の式どおりで、閾値超過ルールが CX-04 の無効化機構で外れること。排除履歴(`rule_eliminations`)が残り図鑑の「排除済み」に出ること。誤排除からの手動復活が CLI で動くこと |
| 4 | **E9** 人気度算出(PR-02) | D-2(仮値で着手可)・E-6(tx 内再計算の契約) | 評価→人気度→優先度の変換が E09 §3.2 どおりで、反映タイミングが定義どおりであること。人気度・優先度の現在値を開発者が確認できること。**E11 の本有効化**(RV-01 並び順・RV-02 の人気度/優先度/排除済み表示)をここで閉じる |
| 5 | **E10** 成功指標の計測(OP-03) | D-3(仮値で着手可)・B-4・E-10(決定済み: 都道府県カバーの正準集計を共有利用) | 「面白かった」率がルール数推移と重ねて見られること。実装済み/排除数・都道府県カバー数が E10 §5 の読み取り契約(最小カラム)の範囲で集計されること。既存 OP-01/02 の CLI 基盤(読み取り専用)に載せること |
| 6 | **E10** カオス化の定期観測・チューニング(OP-04) | D-1(θ/N_min/z の較正手順) | 運用手順書として成立していること(「面白かった」率低下時に何をどの順で動かすか)。手順が OP-03 の実出力と対応していること。新しい常駐機構を作らないこと |

## 並行可否

- **#1(EV-01→02)と #2(PR-01)は最初から並行できます**(PR-01 の依存は CX-03 のみ)。
- #3(EV-03)は #1 の後、#4(PR-02)は #2・#1(EV-02)の後。**#3 と #4 は並行できます**。
- #5(OP-03)は #3 の後(排除数の集計に `rule_eliminations` が要る)。#6(OP-04)は最後(#3・#4・#5 が揃ってから)。
- フェーズ 2 の残件(DP-02 仕上げ、E7 の実リハーサル類、E15 発注)とは独立です。ただし **E15(AU-01〜03)が並行で進む場合、`users` まわりのスキーマ変更が重なる**ので、migration の順序だけ調整してください。
