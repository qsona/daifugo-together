# 発注書: フェーズ 2(提案パイプライン)

> 進め方の共通ルールは [implementation-workorder.md](../implementation-workorder.md) を参照。本書はフェーズ 2 の発注記録である(大半は完了済み。現況の正は `docs/impl-progress.md`)。

フェーズ 1 と同じく「プロセス1 → 方向性レビュー → プロセス2 → 完了レビュー」で回します。並び順の設計意図はバックログ §4「並び順の設計意図」のフェーズ 2 の段落が正です(企画書 §7 の流れ: 受付 RP → 検査 YC → 可否判断・実装・検証 CX)。

**着手前の前提(最初に確認してください)**:

- **E07 設計書は開発者レビュー済みです(2026-07-27)。** 「人間承認駆動」へ全面改訂のうえ承認されました(可否判断はサーバー内ジョブ、実装は開発者が skill を起動、マージ・有効化は開発者。常駐ワーカーは廃止)。decision-log **C-5**(リトライモデル)は E7 内包モデルで決定済み、**A-4**(codex ヘッドレス規約)は人間起動運用により解消。裁定記録は decision-log G-8〜G-10 です。着手前に必ず改訂版 E07 の冒頭ノートから読んでください。なお E05・E06 など設計書ヘッダの「提案(レビュー待ち)」表記は更新漏れです — 着手可否はバックログ §2 の索引注記で判断してください。
- **TS-01 の残決定はなくなりました**(リポジトリ公開範囲は A-2 で public 決定済み、A-4 は上記のとおり解消)。ただし **C-7(codex 実行の隔離水準)は CX-02 の運用開始前に決定が必要**です。CX-02 の開発自体は FakeCodexRunner で先行できます。
- **decision-log は C 節だけでなく、C・D・E 節を「宛先」欄で自分のストーリー ID を検索して読むこと。** C-1(イエローカード停止期間)は **YC-02・YC-03 の前提**です(E06 に推奨値あり。開発者承認を早めに取ってください)。C-9(SQLite バックアップ)はフェーズ 2 外へ保留になり、CX-02 のブロックは解除済みです。**C-2 の決定(LLM 判定はローカル判定ツール+codex app-server、受理不受理は非同期)は E07 本文と E06・E05 の冒頭ノートに反映済み**です。E06・E05 の本文には旧前提(送信時同期検査・遮断は行を作らない等)が残っているので、**冒頭ノートを正として読み替えてください**(E-18)。
- **E13 は DP-01(本番構築)・DP-03(ロールバック)まで本番検証済みです。** 残っているのは **DP-02 の仕上げのみ**: GitHub Environment `production` の作成と `FLY_API_TOKEN` の登録、main push 起点の初回 CD 実行確認(手順は `docs/runbooks/E13-production.md`)。これらは開発者の権限が要るので早めに依頼してください。CX-05(ルール反映=デプロイ)はこの CD の実働が前提です。

| # | Epic / ストーリー | 関係する未決 | レビューの重点 |
|---|---|---|---|
| 1 | **E5** 提案受付(RP-01 → 02。**RP-03 は依存に CX-02 を含む**ため、#3 のあとに戻って仕上げる) | C-3・C-5・C-6 は決定済み(**E05 冒頭ノートを正として読む**)・C-4(決定済み。実装時に再確認)・**E-14(ルール名 12 文字のリボン制約と C-3 の 40 文字の整理。RP-01 実装時に確定)**・E-8 | 提案の状態モデル(E05 §2.2)との整合。送信は非同期受付(レート制限なし・同期検査なし)であること(E-18) |
| 2 | **E6** 検査+イエローカード(YC-01 → 02 → 03) | C-1(開発者承認が必要)・**E-18(実行方式の再構成。E06 冒頭ノートを正として読む)** | L3 がローカル判定ツールで CX-01 の**前**に走る構成(検出設計の所有は E6 のまま)。誤検出時の扱い。演出のトーンガイド準拠 |
| 3 | **E7** codex パイプライン(CX-01 → 02 → 03 → 04) | C-2・C-5・C-6・C-7 は決定済み、C-8 は凍結、C-9 はフェーズ 2 外へ | 生成コードの検証境界と `packages/rules/` 差分ガード、SPEC 承認レビューの動線、ルール単独ロールバック。人間承認駆動(改訂版 E07 §2.2/§2.3)の駆動モデルどおりに実装すること |
| 4 | **AI-02**(E2)新ルールへの追従 | 企画書 §9-3 | CX-03 完了後、CX-05 までに済ませる。未知ルール下で不正手・進行停止を起こさないこと |
| 5 | **CX-05・CX-06** コア体験の成立 | — | ルール反映がデプロイ経路(DP-02 の CD)で実働すること。発動演出(RuleCutIn)への実データ接続 |
| 6 | **E10** OP-01・OP-02 | D-4(OP-02 の採用率の分母)・E-15(C-6 は決定済み) | E07 の人間承認駆動化と C-2 決定(LLM 判定も subscription)により、OP-01 の頻度上限・コスト上限はほぼ役割を失う(残るのはキュー可視化と判定・失敗の集計 — E07 §2.3)。D-5 は実質不要になった見込み。E10 設計との突き合わせを着手時に行う |
| 7 | **E11** ルール閲覧(RV-01 → 02) | — | CX-05 の後。§4.5 の「ルール名だけのラフ表示」の情報量を守る(図鑑の人気度・優先度表示はフェーズ 3) |
| 8 | **E14** チュートリアル(TU-01 → 02 → 03 → 04。V3 は E14 §3 の a/b/c の 3 段) | C-10・C-11・C-12(いずれも仮値で着手可)・E-19(契約は E14 §2.1 が正) | 契約変更(`RoomMode`)を最小に保つこと。**きほんの部屋が 2 セット目でみんなのルールに化けない回帰テスト**(E14 V1)。ガイド文言の増殖抑制(UI文言ガイド §3 の削除テスト。7 個の cue 全部を実装する義務はない) |

並行してよいもの: YC-02・YC-03 は YC-01 完了後なら CX 系と並行できます。OP-01 は CX-02 と同時期に必須です。**E14(TU-01〜04)は提案パイプラインに依存しないため、#1〜7 のどの段階とも並行できます。**
