# 実装ガイド

このプロジェクト「みんなでつくろう 大富豪」の実装を担当していただきます。設計は一通り終わっていて、Epic ごとの設計書とバックログが揃っています。**設計の意図を汲みつつ、良い実装に着地させることを期待しています。**

---

## 1. 何を作るか

- **プロダクト**: ユーザーがルールを提案すると AI が実装し、ルールが増えるほどカオスになっていく大富豪。カオスそのものを楽しんでもらうゲームです。
- **今回の担当**: 発注時に指定された Epic とストーリー。
- **全体像**: [product-backlog.md](product-backlog.md) に Epic とユーザーストーリーが優先順に並んでいます。

## 2. 読んでおくもの

| ドキュメント | 位置づけ |
|---|---|
| `docs/epics/E12-tech-stack.md` | 技術スタック・アーキテクチャ・ルールプラグイン契約。**全 Epic 共通の前提** |
| `docs/epics/E{担当}.md` | 担当 Epic の詳細設計。**冒頭の改訂反映ノートから読んでください**(後の決定による読み替えが書いてあります) |
| [product-backlog.md](product-backlog.md) | 担当ストーリーの本文と受け入れ条件、依存関係 |
| [decision-log.md](decision-log.md) | まだ決まっていないことの一覧。「宛先」欄に自分のストーリー ID があるものは目を通してください |
| `docs/impl-progress.md` | 実装の進捗記録。前任の作業がここに残っています(なければ §6 の形式で作ってください) |

情報が食い違っていたら、**設計書 > 台帳**、設計書どうしなら **E12 > 個別 Epic** を正としてください。食い違い自体は報告してもらえると助かります。

## 3. 進め方

一度で完璧に仕上げるのではなく、途中でレビューのプロセスを挟みます(レビュー側の観点は [review-guide.md](review-guide.md) にあります)。

**プロセス1: 縦に通す**
担当ストーリーが「ユーザーストーリーとして成立する」状態まで持っていきます。「〜として、〜したい」が実際に動いて確認できればこの周は達成です。**受け入れ条件を全部満たす必要はありません。** エッジケース・エラー処理・網羅的なテストはプロセス2に回して構いません(回したものは記録してください)。

ここで一度レビューが入ります。見るのは主に「**この方向で作り込んでよいか**」と「**置いた仮定が妥当か**」です。

**プロセス2: 仕上げる**
レビュー指摘と仮定の裁定結果を反映し、受け入れ条件を満たし切ります。エッジケースとエラー処理もここで仕上げます。

Epic ごとに、プロセス2が完了したら main ブランチにチェックインしてください。

## 4. 判断の基準

設計書は「ミドル以上のエンジニアが追加質問なしで着手できる」ことを目指して書いていますが、当然すべては書ききれていません。**書かれていないことは、あなたの判断で決めて進めてください。** 実装の詳細、ファイル分割、テストの書き方、命名の細部、リファクタの判断などは、あなたの領域です。

一方で、**勝手に変えないでほしいもの**が 3 つあります。ここを動かすと他の Epic が連鎖的に壊れるためです:

1. **設計書に定義された契約** — 型定義、フック、Effect 語彙、状態モデル、API の形。変える必要を感じたら、変えずに提案として報告してください(その判断自体は歓迎です)
2. **設計書そのもの**(`docs/epics/*.md`) — 誤りや不足を見つけたら、書き換えずに報告してください
3. **decision-log の決定状態** — 決定は人間が行います

オーバーエンジニアリングをしないように注意してください。入れた方がよいと考える機構があれば、実装する前に提案してください。

## 5. 未決事項に当たったら

設計書の意図と企画書に整合する範囲で、妥当な仮定を置いて進めてください。

**置いた仮定は必ず記録してください**(§6)。記録してあれば、後で覆っても事故になりません。記録がないまま埋め込まれた判断が一番危険です。

なお、`decision-log.md` に「仮値で着手可」と書いてある項目は、そのまま進めて構いません。

## 6. 記録の残し方(`docs/impl-progress.md`)

長時間の作業になるので、**セッションが切れても再開できる**ことが重要です。リポジトリを見れば状況が分かる状態を保ってください。以下は最低限あってほしい項目で、形式は自由です。

```md
# 実装進捗

## 現在
- Epic: E1 / 周: 1 周目 / ストーリー: GE-02 / 次にやること: <1 行>

## 完了したストーリー
| ストーリー | 周 | コミット | 検証結果 |
|---|---|---|---|

## 置いた仮定(レビューで裁定してもらう)
| 仮定した内容 | なぜそう決めたか | 出典 | 覆ったときの影響範囲 |
|---|---|---|---|

## 2 周目に回したもの
| ストーリー | 内容 | 理由 |
|---|---|---|

## 詰まっている点(人間の判断待ち)
```

「置いた仮定」と「2 周目に回したもの」は、レビュアーが直接使います。ここが薄いとレビューの精度が落ちるので、丁寧にお願いします。

## 7. 止まってほしいとき

次の場合だけ、記録を残して報告してください:

- 設計書どうしが矛盾していて、**何を作るべきかが定まらない**(仮定を置くことすらできない)
- 同じ問題で 3 回以上やり直している(手を変えても抜けられない)
- **契約を変更しないと実装できない**
- 担当範囲外への変更が必要になった
- 環境や権限の問題で作業自体が進まない

## 8. 報告

プロセスを終えたら、次を教えてください。他にも必要な情報があれば追加してください。

- 完了したストーリーとコミット
- 型検査・lint・テストの実行結果(**実際に走らせた出力**でお願いします)
- **置いた仮定**(裁定してほしい順に)
- (プロセス1では)意図的にプロセス2に回したもの
- 設計への提案・気づいたこと
- 詰まっている点

1 周目の報告では、**「ユーザーストーリーが動くことをどう確認したか」**(テスト名、手元での動作手順など)を書いてください。レビュアーが最初に見る情報です。

---

## フェーズ 1 の進行順

各 Epic を「プロセス1 → 方向性レビュー → プロセス2 → 完了レビュー」で回します。関係する未決事項は決まっていなくても着手可能です(仮定 → 裁定の流れで吸収します)。

| # | Epic | 関係する未決 | レビューの重点 |
|---|---|---|---|
| 1 | **TS-02** モノレポ雛形・CI・差分ガード | — | 以後の全作業の足場。プロセスを分けず 1 回のレビューで十分 |
| 2 | **E1** ゲームエンジン(GE-02 → 03 → 05 → 04) | B-1・B-2・B-4 | 全システムの土台。契約からの逸脱を厳しく |
| 3 | **E2** 対戦 AI(AI-01) | B-7 | worker_threads 構成と探索が予算内に収まるか |
| 4 | **E3** マルチプレイ(MP-01 → 02) | B-3・B-5・B-6 | 情報漏えい(手札・private)と二重手番の防御 |

GE-01(基本ルールの明文化)は完了済みです(`E01-game-engine.md` §3.1(b) の確定版が成果物)。

**フェーズ 1 の状況(2026-07-27)**: TS-02・E1(GE-02〜05)・E2(AI-01)・E3(MP-01/02)・E4(DS-01/02)は main に統合済み、E13 は本番デプロイと動作検証(DP-01・DP-03)まで完了しています(`https://daifugo-together.fly.dev/`)。現況の正は `impl-progress.md` です(`product-backlog.md` の `状態:` 行は一部未追記のまま)。

## フェーズ 2 の進行順

フェーズ 1 と同じく「プロセス1 → 方向性レビュー → プロセス2 → 完了レビュー」で回します。並び順の設計意図はバックログ §4「並び順の設計意図」のフェーズ 2 の段落が正です(企画書 §7 の流れ: 受付 RP → 検査 YC → 可否判断・実装・検証 CX)。

**着手前の前提(最初に確認してください)**:

- **E07 設計書は開発者レビュー済みです(2026-07-27)。** 「人間承認駆動」へ全面改訂のうえ承認されました(可否判断はサーバー内ジョブ、実装は開発者が skill を起動、マージ・有効化は開発者。常駐ワーカーは廃止)。decision-log **C-5**(リトライモデル)は E7 内包モデルで決定済み、**A-4**(codex ヘッドレス規約)は人間起動運用により解消。裁定記録は decision-log G-8〜G-10 です。着手前に必ず改訂版 E07 の冒頭ノートから読んでください。なお E05・E06 など設計書ヘッダの「提案(レビュー待ち)」表記は更新漏れです — 着手可否はバックログ §2 の索引注記で判断してください。
- **TS-01 の残決定はなくなりました**(リポジトリ公開範囲は A-2 で public 決定済み、A-4 は上記のとおり解消)。ただし **C-7(codex 実行の隔離水準)は CX-02 の運用開始前に決定が必要**です。CX-02 の開発自体は FakeCodexRunner で先行できます。
- **decision-log は C 節だけでなく、C・D・E 節を「宛先」欄で自分のストーリー ID を検索して読むこと。** 特に **C-9(SQLite バックアップ)は CX-02 の着手をブロック**、C-1(イエローカード停止期間)は **YC-02・YC-03 の前提**です(E06 に推奨値あり。開発者承認を早めに取ってください)。
- **E13 は DP-01(本番構築)・DP-03(ロールバック)まで本番検証済みです。** 残っているのは **DP-02 の仕上げのみ**: GitHub Environment `production` の作成と `FLY_API_TOKEN` の登録、main push 起点の初回 CD 実行確認(手順は `docs/runbooks/E13-production.md`)。これらは開発者の権限が要るので早めに依頼してください。CX-05(ルール反映=デプロイ)はこの CD の実働が前提です。

| # | Epic / ストーリー | 関係する未決 | レビューの重点 |
|---|---|---|---|
| 1 | **E5** 提案受付(RP-01 → 02。**RP-03 は依存に CX-02 を含む**ため、#3 のあとに戻って仕上げる) | C-3(仮値で着手可)・C-4(決定済み。実装時に再確認)・C-5(E7 レビューまでは仮定を置いて進め、裁定を受ける)・**E-14(ルール名は 12 文字以内が必要。C-3 の仮値 40 文字と衝突。RP-01 実装時に確定)**・E-8 | 提案の状態モデル(E05 §2.2)との整合。入力上限・レート制限が E6 §5.2 と食い違わないこと |
| 2 | **E6** 検査+イエローカード(YC-01 → 02 → 03) | C-1(開発者承認が必要)・C-2 | YC-01 が「提案文が codex に渡る**前**」の関門である構成。誤検出時の扱い。演出のトーンガイド準拠 |
| 3 | **E7** codex パイプライン(CX-01 → 02 → 03 → 04) | C-6・**C-7(運用開始前に決定)**・**C-9(CX-02 をブロック)**(C-5 は決定済み、C-8 は凍結) | 生成コードの検証境界と `packages/rules/` 差分ガード、codex 実行の隔離(C-7)、ルール単独ロールバック。人間承認駆動(改訂版 E07 §2.3)の駆動モデルどおりに実装すること |
| 4 | **AI-02**(E2)新ルールへの追従 | 企画書 §9-3 | CX-03 完了後、CX-05 までに済ませる。未知ルール下で不正手・進行停止を起こさないこと |
| 5 | **CX-05・CX-06** コア体験の成立 | — | ルール反映がデプロイ経路(DP-02 の CD)で実働すること。発動演出(RuleCutIn)への実データ接続 |
| 6 | **E10** OP-01・OP-02 | C-3・C-2・C-6・D-4(OP-02 の採用率の分母)・E-15 | E07 の人間承認駆動化により OP-01 の役割が変わる(codex の頻度上限は人間ペースで自然律速。残るのは判定 LLM のコスト上限とキュー可視化 — E07 §2.3)。D-5 の上限較正は判定 LLM 分のみ。E10 設計との突き合わせを着手時に行う |
| 7 | **E11** ルール閲覧(RV-01 → 02) | — | CX-05 の後。§4.5 の「ルール名だけのラフ表示」の情報量を守る(図鑑の人気度・優先度表示はフェーズ 3) |

並行してよいもの: YC-02・YC-03 は YC-01 完了後なら CX 系と並行できます。OP-01 は CX-02 と同時期に必須です。
