# 実装進捗

## 現在

- Epic: E4 / 周: プロセス1 完了 / ストーリー: DS-01 / 次にやること: プロセス1 のレビューを受け、指摘と仮定の裁定を反映してからプロセス2(DS-01 の残り + DS-02)へ

## 完了したストーリー

| ストーリー | 周 | コミット | 検証結果 |
|---|---|---|---|
| TS-02 | 単回 | `bbb0cf3` | `pnpm verify` 成功。format / lint / typecheck / 4 tests / 6 packages build がすべて成功 |
| DS-01 | プロセス1 | この行を含むコミット | `pnpm verify` 成功(format:check / lint / typecheck / **10 tests** / 6 packages build)。ブラウザ実描画で 375・768・1280 の 3 ブレークポイントを目視確認(下記) |

### DS-01 プロセス1「ユーザーストーリーが動くこと」の確認方法

ユーザーストーリー: 「プレイヤーとして、開始画面で桃太郎電鉄のようなポップなキービジュアルに迎えられたい」

**自動テスト** — `packages/web/src/App.test.tsx`(6 本、jsdom + Testing Library)

| テスト名 | 検証内容 |
|---|---|
| 起動時にタイトル画面のキービジュアルが表示される | `role="img"` の名前が「みんなでつくろう 大富豪…」で、`src` が `key-visual-2a.svg` を指す |
| 画面全体が「はじめる」ボタンとして支援技術に露出する | `role="button"` / アクセシブル名「はじめる(タップして進む)」 |
| クリックでメニュー画面(1b)へ進む | 画面 1b の見出しが出て、KV が消える |
| キーボード(Tab → Enter)でもメニュー画面へ進む | Tab で全面ボタンにフォーカスが乗り、Enter で遷移 |
| キーボード(Space)でもメニュー画面へ進む | Space で遷移 |
| タイトル画面にメニュー項目を置かない | 「あそぶ」「ルール提案」「ルール図鑑」が画面 1a に存在しない(企画書 §6 のメニュー分離) |

**手元での動作手順**

```bash
pnpm --filter @daifugo/web dev
```

ブラウザで表示 → タイトル画面に 2A キービジュアルが全面表示 → 画面のどこをクリックしても画面 1b へ遷移。

**ブラウザでの目視確認(実施済み)** — dev サーバーを起動して以下を確認した。E04 §3.1(d) は「本 Epic はブラウザツール不使用」を前提に書かれているが、この環境では実描画できたため前倒しで実施した。

| 幅 | 結果 |
|---|---|
| 375×812(スマホ縦) | KV が横幅いっぱい。ロゴ・コピー「毎日どこかで、新ルール。」・「タップしてはじめる」ピルがすべて判読可能。上下の余りはベース色 `--color-bg-brand` で埋まり、放射光・丘が自然に続く |
| 768×1024(タブレット) | 同上。重要 3 要素の見切れなし |
| 1280×800(PC) | 高さ基準で収まり、左右がベース色でピラーボックス。重要 3 要素の見切れなし |

コンソールエラーなし。遷移後の画面 1b もトークン(クリーム地・紺文字・丸ゴシック)が効いていることを確認。実機のノッチ/ホームインジケータでの切れ方は未確認(E04 §3.1(d) のとおり実装着手時に持ち越し)。

## 置いた仮定(レビューで裁定してもらう)

| # | 仮定した内容 | なぜそう決めたか | 出典 | 覆ったときの影響範囲 |
|---|---|---|---|---|
| 1 | **デザイン資産(`design-tokens.css` / `key-visual-2a.svg`)を `packages/web` にコピーせず、Vite の alias `@design` で `docs/design/` を直接参照する** | E04 §2.1.1 は「`src/assets` へコピー」と書くが、同期手順は TS-02 に委譲されたまま未決着。§2.2(3) は相対 import / コピー / 内部パッケージの 3 案を挙げ「正本を 1 つに保つ」ことを要件としている。直接参照ならコピーが存在しないので drift も一致検査も不要になり、要件を構造的に満たす | E04 §2.1.1・§2.2(3) | `packages/web/vite.config.ts` の alias 数行 + `tsconfig.json` の paths。コピー方式へ戻す場合は同期スクリプト 1 本と CI ジョブ 1 つの追加で済む |
| 2 | **ルータ(react-router 等)を導入せず、Zustand の画面 state で 13 フレームを切り替える** | E12 §4.2 が Next.js の棄却理由として「ルーティング等は本ゲームに不要」と明記しており、設計意図が router-less。招待 URL による deep link が必要になるのは E3 の決定事項(B-6 近傍) | E12 §4.2、decision-log B-6 | `packages/web/src/store/screen.ts` 1 ファイル。ルータ導入時は画面コンポーネントをそのまま route にぶら下げるだけ |
| 3 | **DS-02 の 6 フレームは「全データを props で受ける presentational 画面」として作り、state 接続と通信は E1/E3 に委ねる** | DS-02 の依存 GE-02 が未着手のため state 接続は物理的に不可能。E04 §3.2(b) は「素ワイヤーをトーン適用して置き換える」ことを DS-02 の直接責務としており、部品ライブラリ止まりでは受け入れ条件 1 が検証不能になる。props の形はエンジン型(E01 の状態モデル)を写さず、画面が表示する内容だけの view-model 形にする | E04 §3.2(b)・§3.2(d)、product-backlog DS-02 | プロセス2 の成果物全体。E1/E3 が画面を書き換える場合も、トーンが部品側に宿っていれば損失は限定的 |
| 4 | **DS-02 受け入れ条件 2「逸脱がないことをレビューで確認済み」は本 Epic では暫定クローズとし、E1/E3 の接続時に E04 §3.2(b) の逸脱チェック手順を再適用して最終クローズする** | フェーズ 1 の進行順(implementation-workorder 末尾)から E4 は外れており、DS-02 の完了確認が E1/E3 と交差することは元々想定されている | implementation-workorder「フェーズ 1 の進行順」、E04 §3.2(d) | DS-02 の完了判定。最終クローズを E1/E3 の受け入れに積む必要がある |
| 5 | **UI 本文フォントは Web フォント(M PLUS Rounded 1c 400/700/800)をセルフホストで同梱する**(プロセス2 で実施) | E04 §5-3 は同梱可否の判断のみを求め、フォント選定はデザインシステム成果物の責務としている。そのデザインシステム.md §2.1 が同梱を推奨済み。外部 CDN(実行時の外部依存)は E12 の運用物最小方針に反するので採らない | E04 §5-3、デザインシステム.md §2.1、E12 §2 | import 1 行と woff2 アセット。覆れば削除するだけで、フォールバック設計により成立し続ける |
| 6 | **favicon のモチーフは赤・金・緑の 3 色意匠とし、OGP は当面 9:16 を 1200×630 に中央配置してベース色で左右を埋める暫定版とする**(プロセス2 で実施) | 16〜32px で漢字 1 文字は潰れやすく、抽象意匠がフォント非依存で最も安全。OGP の暫定案は E04 §2.1.2 が自ら明記している | E04 §2.1.2・§5-2・§5-5 | 生成スクリプトの再実行 1 回。差し替えは安い |
| 7 | **`packages/web` をライブラリ型からアプリ型へ転換した**(`exports` と `tsconfig.build.json` を削除、`build` を `vite build` に変更、`src/index.ts` スタブを削除) | E12 §4.2 の「React + Vite の SPA」を実体化するため。TS-02 の雛形はまだ空の TS パッケージだった | E12 §4.2、TS-02 の現状 | `packages/web/package.json` / `tsconfig.json`。ルート `pnpm build` は緑を維持している |
| 8 | **ルートの vitest を 3 系から 4 系へメジャー更新した** | Vite 8 と `@vitejs/plugin-react` 6(現行の既定)は vite ^8 を peer に要求する一方、vitest 3 は vite 7 を持ち込み、同一 TS プログラム内で 2 つの vite 型が衝突した。vitest 4 は vite ^8 に対応する。既存の `scripts/check-rule-diff.test.ts` は 4 系でも変更なしで通っている | TS-02 の CI、vitest / plugin-react の peer 依存 | ルート `package.json` と `vitest.config.ts`。TS-02 領域への変更なので裁定対象 |
| 9 | **`skipLibCheck` を `packages/web` に限定して有効化した** | pnpm が vitest 側と web 側で別インスタンスの vite を解決するため、`node_modules` 内の .d.ts どうしが衝突する。自前コードとは無関係で、Vite 公式テンプレートも同じ設定。共有の `tsconfig.base.json` は変更していない | Vite 公式テンプレート、typecheck の実出力 | `packages/web/tsconfig.json` 1 行。vite インスタンスの重複が解消されれば外せる |
| 10 | **`index.html` の `<meta name="theme-color">` にだけ生の 16 進カラーを書いた** | CSS カスタムプロパティが使えない箇所で代替がない。E04 §2.3(3) のトークン準拠検査は `packages/web/src` 配下の CSS/TSX が対象なので、`index.html` は検査スコープ外 | E04 §2.3(3) | `packages/web/index.html` 1 行。トークン値の変更時に手で追随する必要がある(プロセス2 の検査で追随漏れを拾えるようにするか要検討) |
| 11 | **視覚的に隠すユーティリティ `.sr-only` の 1px 系の値はトークン化しない** | clip-path 方式の sr-only は確立したイディオムで、1px はレイアウトの寸法ではなく実装値。`global.css` の 1 箇所に閉じている | E04 §2.3(2)、一般的な a11y 実装 | `packages/web/src/styles/global.css`。プロセス2 の stylelint ルールでこの箇所を明示的に除外する必要がある |

## 2 周目に回したもの

| ストーリー | 内容 | 理由 |
|---|---|---|
| DS-01 | ロゴ「大富豪」のアウトライン(パス)化した派生 SVG(E04 §5-1) | 端末字形依存の解消。実グリフでのパス化はブランドの核の見えを固定する行為で、忠実度の合否は人間の目視承認が要る。プロセス1 はライブテキスト版で成立する |
| DS-01 | Web フォント(M PLUS Rounded 1c)の同梱 | 仮定 5。トーンの忠実度に関わるが、プロセス1 のストーリー成立には不要 |
| DS-01 | favicon(4 サイズ)/ OGP 画像の事前生成とコミット(E04 §2.1.2) | 仮定 6。共有導線の仕上げでありストーリー成立には不要 |
| DS-01/02 | CI のトークン準拠検査(生 16 進カラー・色関数・レイアウト外 px 直値の検出、E04 §2.3(3)・§4-1) | トーン崩れの機械的防止。守る対象(画面と部品)がまだ少ないので、DS-02 の実装と同時に入れるのが自然 |
| DS-01 | キービジュアル SVG の静的検証 CI(`xmllint --noout` + 外部参照 grep、E04 §4-2) | 同上。派生 SVG(アウトライン版・favicon・OGP)が出てから一括で入れる方が手戻りが少ない |
| DS-02 | design-system.html の 15 部品の React 化と、6 フレーム(1b・2a・2b・3・5a・5b)のトーン適用 | DS-02 本体。プロセス1 は DS-01 の縦通しに絞った |
| DS-02 | インゲーム部品(Card・HandTray・FieldArea・PlayerSeat・Log・RankRow)の見本を design-system.html に追加 | 下記「詰まっている点」4 参照。デザインシステム成果物はアウトゲーム専用で、画面 3・5a・5b に要る部品がカタログに存在しない |

## 詰まっている点(人間の判断待ち)

1. **E04 設計書の状態が「提案(開発者の承認待ち)」のまま**で、`decision-log.md` A 節に E04 承認の記録がない(A-1 は E12 のみ)。発注があった以上着手は正当と判断して進めたが、承認は開発者の行為なので今回のレビューサイクルで閉じてほしい。
2. **DS-01 受け入れ条件 3「開発者が承認済み」は実装担当には閉じられない。** 記録先が E04 付録「承認記録」= 設計書であり、設計書は実装作業で変更しない契約(implementation-workorder §4-2)。承認日・対象(`key-visual-2a.svg` + トークンのコミットハッシュ)・承認者の記入を開発者にお願いしたい。
3. **E04 §5 の未決事項 6 件が `decision-log.md` に登録されていない。** 他 Epic の未決は B/C/D 節に昇格済みだが E04 分だけ漏れている(review-guide §7 により台帳反映はレビュー側の作業のため、指摘のみ)。
4. **インゲーム部品がデザインシステム成果物に存在しない。** `デザインシステム.md` 冒頭が「インゲーム専用部品(手札・場・発動演出)は本書の対象外」と明言しており、design-system.html の 15 部品はすべてアウトゲーム用。E04 §3.2 の対応表が要求する Card / HandTray / FieldArea / PlayerSeat / Log / RankRow はカタログに無く、DS-02 が新規に足す側になる。これは新規のデザイン作業なので、プロセス2 のレビューで開発者の目視承認を関門にしたい。DS-02 の実作業量の重心もここにある。
5. **キービジュアル 2A に JOKER カードが描かれているが、素の大富豪 v1(decision-log A-3、2026-07-25 決定)は「ジョーカーを初期から除外(52 枚・全員 13 枚)」としている。** 2A の決定は 2026-07-24 で A-3 より前なので、後の決定と原資産が食い違っている。原資産は勝手に変更しない契約なので報告に留める。フェーズ 2 でジョーカーを追加ルールとして復活させるなら整合するが、フェーズ 1 の見えとしては「ゲームに無いカード」が最初の一画面に大きく出ている状態。
6. **Git remote が無いため GitHub Actions 上での CI 実行は未確認**(TS-02 から継続)。ローカルの `pnpm verify` 相当までは確認済み。
