# Codex へのプロジェクト指示

## 実装完了後のマージ

このプロジェクトで Codex に実装を依頼した場合、実装・必要な検証・レビュー対応が完了したら、原則として `main` へのマージまで行ってよい。マージのためだけに、あらためてユーザーの確認を求める必要はない。

ただし、通常の PR、必須 CI、ブランチ保護など、リポジトリで定められた手順は省略しない。また、ユーザーがマージしないよう明示した場合、未解決の重要な指摘や失敗した必須チェックがある場合、または個別のワークフローで人間によるマージが明示的に必要とされている場合はマージしない。

## 長期作業ブランチへの main の取り込み

長期間にわたって同じ作業ブランチで実装を続ける場合は、機能や工程の完了など、作業の区切りがよい時点で最新の `main` をそのブランチへ取り込む。取り込み方法は、ブランチの共有状況や履歴を考慮し、merge または rebase の適切な方を Codex が選んでよい。
